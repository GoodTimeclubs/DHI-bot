# Entwicklungs-/Test-Abhängigkeiten (nicht im Docker-Image nötig)
#   pip install -r requirements-dev.txt
#   playwright install chromium   # einmalig für die Widget-Tests
pytest>=8
playwright>=1.45
PyYAML>=6
httpx>=0.27
